今日学んだこと
  - linux コマンド
  - クリップボードへのコピー
    - mac
      ```
        cat hoge.txt | pbcopy
      ``` 
    - winddows
      ```
        cat hoge.txt | xsel --clipboard --input
      ```
  - EC2
    - docker 再起動
      ```
        sudo service docker stop
        sudo service docker start
      ```
      - EC2再起動時はこのコマンドで再起動を行う
    - growiのdocker compose up時のフリーズ対策
      - docker-compose.ymlのファイル修正
        ```
          bootstrap.memory_lock: false
        ```
      - [参考サイト](https://ja.tak-cslab.org/archives/3064)
  - 用語理解
    - スワップ
      - コンピュータのメモリ管理に関する概念
      - 物理メモリ（RAM）とディスクストレージ（ハードディスクやSSDなど）との間でデータを移動するプロセス