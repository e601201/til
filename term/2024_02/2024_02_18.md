今日学んだこと
- Rails 
  - formヘルパー
    - collection_select
      - 例 : <%= form.collection_select :city_id, City.order(:name), :id, :name %>
    - collection_radio_buttons
      - 例 : <%= form.collection_radio_buttons :city_id, City.order(:name), :id, :name %>
    - collection_check_boxes
      - 例 : <%= form.collection_check_boxes :interest_ids, Interest.order(:name), :id, :name %>
