マイグレーション：「移行」とか「移動」とか、そんな感じの意味の用語であり古いやつに入っているデータとかプログラムとかを新しいやつに移すことです。

https://koumi-test.com/realms/oidc-sample/protocol/openid-connect/auth
?response_type=code
&client_id=rp1
&redirect_uri=http://localhost:3000/oidc&scope=openid

docker run  --rm --name keycloak_auto_build -p 8081:8080 \
        -e PROXY_ADDRESS_FORWARDING=true \
        -e KEYCLOAK_ADMIN=admin -e KEYCLOAK_ADMIN_PASSWORD=admin \
        quay.io/keycloak/keycloak:latest \
        start \
        --auto-build \
        --db=dev-mem --hostname localhost:8081 --http-enabled true \   
        --hostname-strict-https false

        docker run -d -p 8081:8080 -e KEYCLOAK_USER=admin -e KEYCLOAK_PASSWORD=admin jboss/keycloak:7.0.0



        if (user.role = "1"){
          console.log("adminユーザーです。")
        }